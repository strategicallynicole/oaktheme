routes:
  /:
    controller: channel
    template: 
      - home
  /about/:
    template: 
      - about
collections:
  /blog/:
    permalink: /blog/{slug}/
    template: 
      - index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
