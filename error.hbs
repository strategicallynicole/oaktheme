{{!< default}}
<div class="text-center section-padding">
    <div class="container">
        <div class="error">
            <h2 class="display-2">{{statusCode}}</h2>
            <p>{{message}}</p>
            <a href="{{@site.url}}" class="btn btn-danger mt-4">GO HOME</a>
        </div>
    </div>
</div>